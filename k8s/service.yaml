apiVersion: v1
kind: Service
metadata:
  name: prestige-motors-service
  namespace: luxury-cars
  labels:
    app: prestige-motors
spec:
  type: LoadBalancer
  selector:
    app: prestige-motors
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
